ALGORITHM number words
VAR
    c:= CHAR :=""
    nb_words:INTEGER:=0
    characters:INTEGER:=0
    vowles:INTEGER:=0

BEGIN
write("writ any thing here")
WHILE (c <>".") 
DO read (c)

IF (c=" ") THEN (nb_words ++ )
END_IF

 characters:= characters +1;

IF (c = ("a","o","e","u","i","y")) THEN
    vowles ++

END_IF

END_WHILE
  write("the length of sentence is",characters);
  write("the number of word is" ,nb_word);
  write("the number of vow in the sentence is", vowles)
END





